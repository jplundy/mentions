# Data directory

Place transcript PDFs under `data/pdfs/` and provide a matching inventory file at
`data/inventory.csv` (or JSON) that lists each transcript with metadata. See
`configs/example_pipeline.yaml` and `docs/PIPELINE.md` for details on the expected
schema and workflow.
